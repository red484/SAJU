# 개인화 동작과 연결 범위

## 현재 동작

- 결과 마지막과 홈 미리보기에서 시작 질문을 제안하고 `library.html#profile`로 연결합니다.
- 온보딩의 생년월일로 연령대만 산출합니다. 기존 온보딩 화면과 저장 방식은 변경하지 않습니다.
- 질문은 확인 전 가설입니다. 20대는 취업·진로 상황을 먼저 묻고, 직장인으로 답한 경우 이직을 제안합니다. 40대라는 이유만으로 사업을 한다고 간주하지 않습니다. 사업·프리랜서 답변이 있어야 해당 상황을 확인한 것으로 사용합니다.
- 사용자가 제출한 상황과 고민을 추천 기준으로 사용합니다. 자유 메모는 그대로 보관하며 자연어 AI 분석을 수행했다고 표시하지 않습니다.
- 추천은 확인한 고민, 7일 이내의 개인 일정, 기록별 반응을 사용합니다. `지금은 아니에요`인 기록은 제외하며 모두 제외되면 추천을 쉬고 전체 기록 탐색을 제공합니다.
- 기록 상세에서 반응을 취소할 수 있고 메모를 저장하거나 빈 값으로 지울 수 있습니다. 서재에서는 프로필 수정·삭제, 반응 삭제, 메모 수정·삭제, 최근 읽기 삭제를 지원합니다.
- 달력에서 일정 추가·수정·삭제, 월별 목록, 날짜별 준비 질문과 사용자 메모를 제공합니다. 날짜 선택은 URL에 반영되어 새로고침해도 유지됩니다.

## 데이터

`personal-model.js`는 검증·연령대 질문·추천 로직입니다. `personal-store.js`는 `zero-observatory-personal-v1` 키로 현재 브라우저에 프로필, 반응, 메모, 일정, 최근 읽기를 저장합니다. 생년월일 원문을 이 저장소에 복제하지 않습니다. 저장 차단 환경은 현재 페이지에서 동작하고 저장 실패를 안내합니다. 계정·서버 동기화는 없습니다.

## 실제 사주/AI 연결 전제

현재 사주 계산 및 AI 추론 API는 미연동입니다. 모든 운세 안내는 기존 편집 예시이고, 개인 해석 없는 상태를 사용자에게 명시합니다. 샘플 신화로 사용자 특성이나 고민을 추론하지 않습니다.

실제 개인 사주 해석을 생성하는 백엔드가 준비되면, 결과를 보여주기 전에 같은 세션에 `saju-reading-context`를 다음 계약으로 전달할 수 있습니다.

```json
{
  "mode": "personal",
  "themes": ["career_move", "confidence"],
  "summary": "해석에서 떠오른 주제를 가설로 요약한 문장"
}
```

`themes`는 `personal-model.js`의 concern 키만 허용합니다. `mode: sample`은 무시합니다. `summary`는 표시용이며 추천 점수나 확정 프로필로 변환하지 않습니다. 개인 해석도 질문의 출발점으로만 쓰며 사용자가 확인해야 프로필에 반영됩니다. 실제 AI 연동에는 사주 계산/해석 제공자, 서버 인증·데이터 처리와 동의 흐름이 추가로 필요합니다.

## 검증

`node validate-personal.mjs`는 연령대별 조건부 질문, 샘플 제외, 실제 답변 우선, 부정 반응 제외, 일정 추천, 데이터 검증 및 저장 차단을 검증합니다. `validate-pages.mjs`, `validate-calendar.mjs`, `validate-entry.mjs`, `validate-scroll.mjs`도 함께 실행합니다.
